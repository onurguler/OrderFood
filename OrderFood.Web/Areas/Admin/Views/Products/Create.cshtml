@model ProductDto

@using (Html.BeginForm("Create", "Products", new { area = "Admin" }, FormMethod.Post, true, new { enctype = "multipart/form-data" }))
{
    @await Html.PartialAsync("_Form", Model)
}
