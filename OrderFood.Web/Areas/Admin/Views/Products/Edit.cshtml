@model ProductDto

@using (Html.BeginForm("Edit", "Products", new { area = "Admin" }, FormMethod.Post, true, new { enctype = "multipart/form-data" }))
{
    @await Html.PartialAsync("_Form", Model)
}
